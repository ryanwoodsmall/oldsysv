
   The TIME command displays the time of the day in hours and  minutes
   on the message line.

   To display the TIME use one of the following options:

      - Command Menu - Select TIME + RETURN

      - Typed Command - Type time + RETURN
