#ident	"@(#)sadmin:admin/menu/softwaremgmt/install	1.3"
#	place holder until old documentation advertising "sysadm install"
#	can be updated.
echo '
The command
			sysadm install
is obsolete and may be removed in the future.
			sysadm installpkg
is the correct command, which will now be run for you.
'
exec /bin/sh `dirname $0`/installpkg "$@"
